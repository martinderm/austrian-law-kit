{
  "name": "@openclaw/openclaw-austrian-law",
  "version": "0.1.0",
  "private": true,
  "description": "Skeleton for a native OpenClaw Austrian law plugin (no parser/fetch implementation yet).",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
