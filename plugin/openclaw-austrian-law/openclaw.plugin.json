{
  "id": "austrian-law-kit",
  "name": "Austrian Law Kit",
  "description": "Plugin-Skelett für österreichische Rechtsquellen (ohne Parser/Fetching-Implementierung).",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cacheRoot": {
        "type": "string",
        "description": "Optionaler Override für den Cache-Pfad; ohne Override wird der Workspace des aufrufenden Agenten verwendet"
      },
      "risBaseUrl": {
        "type": "string",
        "description": "Basis-URL für RIS (Platzhalter, noch ungenutzt)",
        "default": "https://www.ris.bka.gv.at"
      },
      "juslineBaseUrl": {
        "type": "string",
        "description": "Basis-URL für JUSLINE (Platzhalter, noch ungenutzt)",
        "default": "https://www.jusline.at"
      },
      "enableJusline": {
        "type": "boolean",
        "description": "Sekundärquelle aktivieren (fachlich weiterhin nur opt-in)",
        "default": true
      }
    }
  }
}
